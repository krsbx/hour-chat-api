import events from './components/chats/events';

// events.groupMessage.createGroupMessageGroup({
//   members: [1, 2],
//   name: 'POPS',
// });

events.groupMessage.removeGroup({
  senderId: 1,
  uuid: 'pbs8ovcuCkOaJ3qBptLH',
  // members: 2,
});

// events.privateMessage.sendPrivateMessage({
//   body: 'UDIN',
//   receiverId: 1,
//   senderId: 2,
// });
